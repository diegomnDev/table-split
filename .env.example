# Enables the "Escanear ticket" button. On Vercel the function lives at the
# app's own origin, so this is a relative path and there is no CORS to set up.
# Leave it empty and the button does not render: manual entry is always the
# primary path, and no image ever leaves the device.
VITE_SCAN_ENDPOINT=/api/scan
