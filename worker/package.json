{
  "name": "table-spit-scan",
  "version": "0.1.0",
  "description": "A Cloudflare Worker that holds the Gemini API key and turns a photo of a restaurant ticket into structured line items.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.129.0"
  }
}
